export interface ISettingsDataForRequest {
  language: string | null,
  theme: string | null,
  render: string | null,
}

export interface IUserDataForRequest {
  username: string | null,
  password: string | null,
}
